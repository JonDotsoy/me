
// eslint-disable-next-line no-undef
describe('render', () => {
  // eslint-disable-next-line no-undef
  it('hola', () => {
    const CanvasBox = require('../src/CanvasBox')
    new CanvasBox().draw()
  })
})
